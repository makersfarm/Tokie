# CLAUDE.md — Tokie

> 미래의 Claude 세션이 코드를 처음부터 안 읽고도 프로젝트 그림을 잡을 수 있게 하는 메모.
> 사람용 상세 문서는 [`docs/`](./docs/README.md) 카테고리별로 분리돼 있음. 항상 거기를 먼저 참고.
> v0.0 까지의 이름은 `token-eater-pet`. v0.1 부터 **Tokie** 로 리네임. 저장 경로(`userData`)는 호환 위해 그대로 `token-eater-pet/` 유지 — `electron/main.ts` 의 `app.setPath` 참고.

## 한 줄

**Tokie** — Claude Code 토큰을 먹고 자라는 Electron 데스크탑 펫. always-on-top 투명 윈도우. statusLine hook(push) + JSONL 파일 감시(pull) 하이브리드로 토큰 수집.

## 문서 카테고리 (`docs/`)

| | 무엇 | 언제 봐 |
|---|---|---|
| [01-tokens.md](./docs/01-tokens.md) | 토큰 수집·dedup·영양 환산 | 새 TokenSource 추가, 영양 가중치 튜닝 |
| [02-pet.md](./docs/02-pet.md) | 상태기계, phase/condition/mood | 진화 임계값, decay 속도, mood 구간 조정 |
| [03-implementation.md](./docs/03-implementation.md) | 부트, IPC, 저장, 테스트, ABI | 새 IPC, 저장 스키마, 테스트 추가 |
| [04-permissions-settings.md](./docs/04-permissions-settings.md) | settings.json 자동 설치, userData, 트레이 권한 | statusLine 설치 동작, Reset/Wipe 정책 |
| [05-pet-design.md](./docs/05-pet-design.md) | SVG 스프라이트, 이펙트 | 아트 교체, mood 표정 추가 |
| [06-ui.md](./docs/06-ui.md) | 윈도우 옵션, 드래그/리사이즈, 메뉴 | UI 동작 변경 |

## Tech stack (핀)

- **Electron 33** — better-sqlite3 ABI 호환. 42 는 V8 API 깨짐.
- **TypeScript 5.7** — strict + noUncheckedIndexedAccess.
- **Vite 6/8** + `vite-plugin-electron`.
- **React 18/19** (renderer).
- **better-sqlite3 12** — native module. WAL.
- **chokidar 5** — ESM only. glob 미지원 → 재귀 dir + extension 필터.
- **vitest 4** — Node 환경.

## 자주 쓰는 명령

```bash
npm run dev          # 펫 윈도우 + HMR
npm run build        # dist-electron + dist
npm test             # 73 tests, vitest
npm run postinstall  # better-sqlite3 → Electron ABI rebuild
```

## 핵심 디렉토리

- `electron/` — main 프로세스. `bootstrap.ts` 가 전체 wiring.
- `core/` — 플랫폼 비의존 로직. 직접 테스트 가능.
- `renderer/` — React + Vite.
- `installers/` — 외부 시스템 손대는 코드.
- `scripts/` — 외부에서 호출되는 stub.
- `docs/` — 6개 카테고리 문서.

## 알려진 함정

### ABI ping-pong (better-sqlite3)

- `npm test` 는 Node ABI 필요. `npm run dev` 는 Electron ABI 필요.
- 전환 시 한쪽 rebuild — [03-implementation.md#abi-함정](./docs/03-implementation.md#abi-함정).

### macOS 메뉴바 트레이

- 시스템 설정 → 제어 센터에서 안 켜져 있으면 안 보일 수 있음. 노치 가려짐도 있음.
- **펫 우클릭** 컨텍스트 메뉴가 fallback — 트레이 안 보여도 모든 동작 가능.

### dev 종료 시 stray Electron

- vite-plugin-electron lifecycle 이슈로 자식 프로세스 가끔 남음.

## 문서 유지 컨벤션 (지속)

> 사용자가 명시적으로 요청한 규칙. 어기지 말 것.

**코드 변경하면 해당 카테고리 문서 같이 업데이트.** PR/커밋 떼기 전에 docs drift 확인.

| 코드 변경 영역 | 같이 봐야 할 문서 |
|---|---|
| `core/tokenSource/`, `installers/statusLine.ts`, `scripts/statusline-shim.cjs`, 영양 가중치 | `docs/01-tokens.md` |
| `core/pet/`, `core/feeding/`, phase/condition 상수 | `docs/02-pet.md` |
| `electron/bootstrap.ts`, `ipc.ts`, `core/storage/`, 테스트 패턴, `package.json` scripts | `docs/03-implementation.md` |
| `installers/`, `core/storage/paths.ts`, tray 권한, Reset/Wipe 동작 | `docs/04-permissions-settings.md` |
| `renderer/public/sprites/`, `renderer/components/{Pet,StageBadge,PetProgressBar,EatingBurst,SpeechBubble,InfoBubble,EvolveCutscene}.tsx`, `renderer/data/speech.ts`, `renderer/hooks/useBurstDetector.ts`, `core/feeding/burstDetector.ts`, `renderer/styles.css` 의 mood/bounce/cutscene | `docs/05-pet-design.md` |
| `electron/{window,tray,ipc,preload,statsWindow}.ts`, `renderer/App.tsx`, hover/click threshold·readout·info bubble 관련 (`renderer/hooks/useHover.ts`, `useTokensToday.ts`), drag/resize 관련 CSS | `docs/06-ui.md` |

새 카테고리가 생기면 `docs/README.md` 의 표와 이 표 둘 다 갱신.

## 사용자 글로벌 규칙 (CLAUDE.md global → 이 프로젝트도 적용)

- **커밋/PR 에 AI 관여 흔적 절대 X** (Co-Authored-By, Generated by 등).
- **git checkout / restore 로 파일 되돌리지 말 것** — 사용자 기존 작업 날아감. Edit 으로 직접 되돌릴 것.
- **단언 금지** — 파급효과, 예상 문제, 놓친 부분 한 번 더 생각하고 확인.
- **파일 언급 시 파일명만** — 사용자가 명시적으로 경로/라인/링크 요구한 경우에만 전체 경로 사용.
- **응답은 한국어**.

## 진입점 (코드)

- `electron/main.ts` — Electron 부팅, lifecycle.
- `electron/bootstrap.ts` — 모든 wiring. 부트 순서는 03 문서 참고.
- `core/pet/PetState.ts` — 펫 FSM.
- `core/feeding/FeedingPipeline.ts` — dedup → 영양 환산 → pet.feed.
- `renderer/App.tsx` — UI 진입점.

## 배포

`.claude/skills/release.md` 참고. electron-builder + GitHub Releases. 트리거 키워드: "release", "ship", "build app", "package", "publish".

## v1 상태

- 24 task plan 완료. 73 tests pass. end-to-end smoke 검증.
- v2 backlog 별도 문서 아직 없음. 추가 작업 합의되면 그때 작성.
