# Contributing

## 개발 환경 셋업

```bash
git clone https://github.com/ikseong/tokie
cd tokie
npm install
npm run dev
```

`npm install` 이 끝나면 `postinstall` 이 `better-sqlite3` 를 현재 Electron ABI 로 자동 rebuild. dev 가 안 뜨면 [docs/03-implementation.md](./docs/03-implementation.md#abi-함정) 의 ABI 함정 절 참고.

## 테스트 / 타입체크

```bash
npm test                # vitest (Node ABI 필요)
npx tsc --noEmit        # 타입체크만
```

**테스트와 dev 를 번갈아 돌리려면** `better-sqlite3` 의 ABI 가 충돌함:

```bash
# 테스트 → dev 로 갈 때
npm run postinstall

# dev → 테스트로 갈 때
npm rebuild better-sqlite3 --update-binary
```

## 브랜치 / 커밋

- master 직커밋도 OK (현재 워크플로우). 큰 변경은 별도 브랜치 + PR 권장.
- 커밋 메시지는 `feat:`, `fix:`, `docs:`, `chore:`, `refactor:` 등 conventional commit 스타일.
- **AI 협업 흔적 절대 X** — `Co-Authored-By`, `Generated by`, `🤖 Generated with` 같은 라인 추가 금지. (CLAUDE.md 글로벌 규칙)

## 문서 유지

코드를 만지면 해당 카테고리 문서도 같이 업데이트:

| 코드 영역 | 같이 봐야 할 문서 |
|---|---|
| `core/tokenSource/`, `installers/statusLine.ts`, 영양가 가중치 | `docs/01-tokens.md` |
| `core/pet/`, `core/feeding/`, phase/condition 상수 | `docs/02-pet.md` |
| `electron/bootstrap.ts`, `ipc.ts`, `core/storage/`, 테스트 패턴 | `docs/03-implementation.md` |
| `installers/`, `core/storage/paths.ts`, tray 권한 | `docs/04-permissions-settings.md` |
| `renderer/public/sprites/`, `renderer/components/Pet*`, 펫 비주얼 CSS | `docs/05-pet-design.md` |
| `electron/{window,tray,ipc,preload}.ts`, `renderer/App.tsx`, UI CSS | `docs/06-ui.md` |

PR 떼기 전에 `git diff` 한번 보면서 docs drift 확인. 자세한 건 [CLAUDE.md](./CLAUDE.md#문서-유지-컨벤션-지속).

## PR 체크리스트

PR 보내기 전:

- [ ] `npm test` 통과 (영향받는 영역).
- [ ] `npx tsc --noEmit` 클린.
- [ ] 관련 docs 업데이트.
- [ ] 커밋 메시지에 AI 흔적 없음.
- [ ] 큰 변경이면 스크린샷 / GIF 첨부 (UI 변경 시 거의 필수).

## 새 기능 제안

큰 변경이면 먼저 **이슈로 디자인 의도 공유** 권장. 작은 버그픽스/문서 수정은 바로 PR OK.

설계 문서 위치: `docs/specs/`. 구현 plan: `docs/plans/`. 둘 다 `YYYY-MM-DD-<topic>-design.md` / `YYYY-MM-DD-<topic>.md` 컨벤션.

## 라이선스

기여한 코드는 [MIT 라이선스](./LICENSE) 로 공개됨에 동의하는 것으로 간주.
